celery_app.conf.update(
    task_serializer="json",
    result_serializer="json",
    accept_content=["json"],
    task_track_started=True,
    result_expires=3600,
    worker_prefetch_multiplier=1,
    # Keep worker alive between tasks
    worker_max_tasks_per_child=100,    # restart after 100 tasks, not after each one
    worker_max_memory_per_child=1500000,  # restart if memory exceeds 1.5GB
)